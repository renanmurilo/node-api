{
  "public": true,
  "version": 2,
  "builds": [
    {
      "src": "bin/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "src/(.*)", "dest": "bin/server.js" }
  ]
}