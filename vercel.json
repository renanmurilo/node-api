{
  "public": true,
  "version": 2,
  "builds": [
    {
      "src": "bin/start.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/$1" },
    { "handle": "filesystem" }
  ]
}